Text field with label + hint. Used in onboarding (name, custom habit) and recipe search.

```jsx
<Input label="¿Cómo te llamamos?" placeholder="Tu nombre" />
<Input label="Hábito personalizado" hint="Lo añadimos a tu pantalla de Hoy" />
```

Never collect weight/height/age — against the product's non-negotiable constraint.
