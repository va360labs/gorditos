Toggle for settings — notifications, preferences. Green when on.

```jsx
<Switch checked={notif} onChange={e=>setNotif(e.target.checked)} label="Tip diario" />
```

Reminder: max 2 notifications/day, never guilt/FOMO copy.
