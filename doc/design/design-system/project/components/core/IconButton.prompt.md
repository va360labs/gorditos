Circular icon-only button (44px hit target) for toolbars, headers and card corners.

```jsx
<IconButton label="Ajustes" bare>⚙️</IconButton>
<IconButton label="Cerrar">✕</IconButton>
```

Always pass `label` for accessibility. Use `bare` when sitting on a colored/header surface.
